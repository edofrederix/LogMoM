probes
{
    type                probes;
    functionObjectLibs  ("libsampling.so");

    writeControl        timeStep;
    writeInterval       1;

    probeLocations
    (
        (0.5 0.5 0.5)
    );

    fields
    (
        N.bubbles
        A.bubbles
        alpha.bubbles
    );
}
