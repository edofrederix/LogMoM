FoamFile
{
    format      ascii;
    class       dictionary;
    location    "system";
    object      topoSetDict;
}

actions
(
    {
        name    extraction;
        type    cellSet;
        action  new;

        source  boxToCell;
        box     (0 0 -99) (99 99 99);
    }
);
